#include "main.h"
/**
 * _abs - computes abs thealute value of an int n
 * @n: the int in quuestion
 *
 * Return: the abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
